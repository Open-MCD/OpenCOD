# Open-COD Server

## Setup

1. Install dependencies:

```powershell
npm install
```

2. Run server:

```powershell
# regular start
npm start
# or for dev with auto-reload (nodemon):
npm run dev
```

Endpoints:
- GET /: cod (html only, not responsive right now)